# Server configuration
# Copy this file to `.env` if you want to override the defaults.
# (No API keys are required — speech recognition and translation run
#  in the browser via the Web Speech API and Chrome's built-in Translator.)

# Port the server listens on (default: 5001)
PORT=5001

# Node environment
NODE_ENV=development
